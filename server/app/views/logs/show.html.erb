<h1>Detalhes do Log</h1>

<p><strong>Animal:</strong> <%= @pet.name %></p>
<p><strong>Abrigo:</strong> <%= @shelter.name %></p>
<p><strong>Criado por:</strong> <%= @log.created_by.email %></p>
<p><strong>Data:</strong> <%= @log.created_at.strftime('%d/%m/%Y %H:%M') %></p>
<p><strong>Conteúdo:</strong> <%= @log.content %></p>

<%= link_to 'Editar', edit_shelter_pet_log_path(@shelter, @pet, @log) if @log.created_by == current_user %>
<%= link_to 'Excluir', shelter_pet_log_path(@shelter, @pet, @log), method: :delete, data: { confirm: 'Tem certeza?' } if @log.created_by == current_user %>
<%= link_to 'Voltar para Logs', shelter_pet_logs_path(@shelter, @pet) %>

